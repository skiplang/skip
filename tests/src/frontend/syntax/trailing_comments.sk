fun f(
  _a: Int, //trailing single comment
  _b: Int, /* trailing delimited comment */
  _c: Int, /*trail1*/ /*trail2 */
  /*
  non trailing */ _d: Int, /* non trailing
   */
): String {
  `//not a comment` // trailing comment
}

fun main(): void {
  print_raw(f(1, 2, 3, 4));
}
