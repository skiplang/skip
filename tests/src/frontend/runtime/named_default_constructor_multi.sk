base class A{x: String = "World"}
base class B{y: String}
class C{a: String, b: String = ", "} extends A, B

fun main(): void {
  c = C{a => "Hello", y => "!\n"};
  print_raw(c.a + c.b + c.x + c.y)
}
