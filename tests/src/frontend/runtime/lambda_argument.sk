fun loopy(end: Int, f: Int -> void): void {
  for (x in Range(0, end)) f(x)
}

fun main(): void {
  loopy(100, _ -> void);
  try {
    void
  } catch {
  | _ -> void
  };
  if (true) void else void;
  print_string("Pass")
}
