# ------------------------------------------------------------------------------
# Skip Project System

add_skip_compiler_executable(skip_depends ${CMAKE_CURRENT_SOURCE_DIR}:skip_depends)

# ------------------------------------------------------------------------------
# Used to type check sources
add_skip_compiler_executable(skip_test ${CMAKE_CURRENT_SOURCE_DIR}:skip_test)
