This directory exists so that generated JS files can require the JS runtime by (absolute) module
name (`require('skip')`). This allows generated JS code to be executed anywhere within the repo's root directory.
